<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="title">Tread Lightly<img class="globeGif" src="https://media3.giphy.com/media/3ohhwgrL4KKPIZoTQY/giphy.gif">
      </div>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-lg-12">
    <form method="POST" action="/search">
      <div class="input-group mb-3 searchBar">
        <input class="country" type="text" class="form-control" name="country" placeholder="Search by Country">
        <div class="input-group-append">
          <button class="btn btn-outline-secondary" type="submit" id="submit">Submit</button>
        </div>
      </div>
    </form>
  </div>
</div>
  <div class="row">
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">What is a carbon footprint?</h5>
          <p class="card-text">A carbon footprint is historically defined as the total emissions caused by an
            individual, event, organization, or product, expressed as carbon dioxide equivalent.</p>
          <a href="https://coolclimate.org/calculator" rel="noopener no referrer" target="_blank" class="calcweb"><img
              src="/images/footprint.jpg" id="eimg" width="45" height="65">
            Carbon Footprint Calculator</a>
        </div>
      </div>
    </div>
      <div class="col-md-8">
        <div class="card">
          <h5 class="card-header">Carbon Footprint Data</h5>
          <div class="card-body">
            <table class="table table-sm table-hover">
              <thead>
                <tr>
                  <th scope="col">Country</th>
                  <th scope="col">UN Region</th>
                  <th scope="col">Year</th>
                  <th scope="col">Carbon Footprint</th>
                  <th scope="col">Percapita GDP</th>
                  <th scope="col">Population(millions)</th>
                </tr>
              </thead>
              <tbody>
                {{#each footprints}}
                <tr>
                  <td class="country">{{this.country}}</td>
                  <td class="unRegion">{{this.unRegion}}</td>
                  <td class="year">{{this.year}}</td>
                  <td class="carbon">{{this.carbon}}</td>
                  <td class="gdp">{{this.perCapitaGDP}}</td>
                  <td class="population">{{this.populations}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        </div>
      </div>
  </div>
    <div class="row">
      <div class="col-md-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">What are global hectares?</h5>
            <p class="card-text"> It is a measurement unit for quantifying both the ecological footprint of people or
              activities as well as the biocapacity of the earth or its regions.
              A hectare is a unit of area equal to 10,000 sq meters. </p>
          </div>
        </div>
      </div>
      {{!-- Research searches table --}}
        <div class="col-md-8">
          <div class="card">
            <h5 class="card-header">Recent Searches</h5>
            <div class="card-body searchHeight">
              <table class="table table-sm table-hover">
                <thead>
                  <tr>
                    <th scope="col">Country</th>
                    <th scope="col">Carbon Footprint</th>
                  </tr>
                </thead>
                <tbody>
                  {{#each RecentSearches}}
                  <tr>
                    <td class="country">{{this.dataValues.country}}</td>
                    <td class="carbon">{{this.dataValues.carbon}}</td>
                  </tr>
                  {{/each}}
                </tbody>
              </table>
            </div>
          </div>
        </div>
    </div>